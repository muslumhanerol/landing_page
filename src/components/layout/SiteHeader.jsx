import {FaBars, FaBrain, FaEnvelope} from "react-icons/fa"

export default function SiteHeader({isScrolled, mebuopen, onToggleMenu, onCloseMenu}) {
    return (
        <>
         <header className={`fixed left-1/2 top=0 z-50 mt-3 flex w-[min(95%, 72rem)] -translate-x-1/2 items-center justify-between rounded-2xl border px-4 py-3 backdrop-blur-xl transition-all duration-300`}>


         </header>        
        </>
    )
}